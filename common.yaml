# enable logging
logger:
    esp8266_store_log_strings_in_flash: false
    #level: VERY_VERBOSE

# enable hass api
api:
  password: !secret api_password

# enable ota update
ota:
  password: !secret flash_password
